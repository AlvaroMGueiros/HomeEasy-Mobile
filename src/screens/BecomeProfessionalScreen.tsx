import { Image, StyleSheet, Text, View } from 'react-native'; import { NativeStackNavigationProp } from '@react-navigation/native-stack'; import { useNavigation } from '@react-navigation/native';
import { useAuth } from '../auth/AuthContext'; import { AppButton } from '../components/ui/AppButton'; import { Screen } from '../components/ui/Screen'; import { SectionHeader } from '../components/ui/SectionHeader'; import { RootStackParamList } from '../navigation/types'; import { colors } from '../theme/colors';
export function BecomeProfessionalScreen() { const navigation = useNavigation<NativeStackNavigationProp<RootStackParamList>>(); const { user } = useAuth(); function start() { if (user) navigation.navigate('App', { screen: 'Profile' }); else navigation.navigate('Login'); } return <Screen><SectionHeader eyebrow="Para profissionais" title="Seu trabalho mais perto de quem precisa." description="Crie seu perfil, apresente seus serviços e receba pedidos de clientes da sua região." /><View style={styles.visual}><Image source={require('../../assets/home-easy-logo-v2.png')} style={styles.logo} resizeMode="contain" /><Text style={styles.pro}>Home Easy Pro</Text><View style={styles.request}><Text style={styles.overline}>Novo pedido</Text><Text style={styles.requestTitle}>Limpeza residencial</Text><Text style={styles.text}>Próximo de você · Hoje, 14h</Text></View><View style={styles.metrics}><Text style={styles.metric}>Perfil completo · 100%</Text><Text style={styles.metric}>Avaliação · 4,9 ★</Text></View></View><AppButton label={user ? 'Cadastrar serviço' : 'Criar minha conta'} onPress={start} /><AppButton label="Como funciona" variant="secondary" onPress={() => navigation.navigate('HowItWorks')} /></Screen>; }
const styles = StyleSheet.create({ visual: { padding: 20, gap: 14, borderRadius: 24, backgroundColor: colors.primary }, logo: { alignSelf: 'center', width: 120, height: 100 }, pro: { color: colors.white, textAlign: 'center', fontSize: 20, fontWeight: '900' }, request: { gap: 5, padding: 16, borderRadius: 17, backgroundColor: colors.surface }, overline: { color: colors.accent, fontSize: 11, fontWeight: '900', textTransform: 'uppercase' }, requestTitle: { color: colors.text, fontSize: 18, fontWeight: '800' }, text: { color: colors.textMuted }, metrics: { gap: 8 }, metric: { color: colors.white, fontWeight: '700' } });
