import { StyleSheet, Text, View } from 'react-native'; import { NativeStackNavigationProp } from '@react-navigation/native-stack'; import { useNavigation } from '@react-navigation/native'; import { AppButton } from '../components/ui/AppButton'; import { Screen } from '../components/ui/Screen'; import { SectionHeader } from '../components/ui/SectionHeader'; import { RootStackParamList } from '../navigation/types'; import { colors } from '../theme/colors';
const steps = [{ number: '01', title: 'Encontre', text: 'Busque pelo serviço e veja os profissionais disponíveis na categoria.' }, { number: '02', title: 'Escolha', text: 'Confira informações, valores e avaliações antes de decidir.' }, { number: '03', title: 'Combine', text: 'Converse diretamente, defina data, horário e os detalhes.' }, { number: '04', title: 'Avalie', text: 'Depois do serviço, compartilhe sua experiência com a comunidade.' }];
export function HowItWorksScreen() { const navigation = useNavigation<NativeStackNavigationProp<RootStackParamList>>(); return <Screen><SectionHeader eyebrow="Como funciona" title="Da busca ao serviço concluído." description="Uma experiência direta para encontrar e contratar o profissional ideal." />{steps.map(step => <View key={step.number} style={styles.step}><Text style={styles.number}>{step.number}</Text><View style={styles.grow}><Text style={styles.title}>{step.title}</Text><Text style={styles.text}>{step.text}</Text></View></View>)}<AppButton label="Encontrar um serviço" onPress={() => navigation.navigate('Services')} /></Screen>; }
const styles = StyleSheet.create({ step: { flexDirection: 'row', gap: 16, paddingVertical: 15, borderBottomWidth: 1, borderBottomColor: colors.border }, number: { color: colors.accent, fontSize: 20, fontWeight: '900' }, grow: { flex: 1, gap: 4 }, title: { color: colors.text, fontSize: 19, fontWeight: '800' }, text: { color: colors.textMuted, lineHeight: 21 } });
