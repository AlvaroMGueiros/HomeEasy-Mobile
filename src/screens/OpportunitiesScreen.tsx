import { useEffect, useState } from 'react'; import { StyleSheet, Text, View } from 'react-native'; import { apiRequest } from '../api/api-client'; import { Screen } from '../components/ui/Screen'; import { SectionHeader } from '../components/ui/SectionHeader'; import { StateView } from '../components/ui/StateView'; import { colors } from '../theme/colors'; import { ServiceRequest } from '../types/api';
export function OpportunitiesScreen() { const [items, setItems] = useState<ServiceRequest[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { apiRequest<ServiceRequest[]>('/marketplace/opportunities').then(setItems).catch(currentError => setError(currentError.message)).finally(() => setLoading(false)); }, []); return <Screen><SectionHeader eyebrow="Para profissionais" title="Oportunidades" description="Solicitações compatíveis com seus serviços e sua região." />{loading && <StateView loading message="Buscando oportunidades..." />}{Boolean(error) && <StateView message={error} />}{!loading && !error && !items.length && <StateView message="Nenhuma oportunidade disponível agora." />}{items.map(item => <View key={item.id} style={styles.card}><Text style={styles.title}>{item.service?.name || 'Serviço'}</Text><Text style={styles.description}>{item.description}</Text><Text style={styles.meta}>{item.city}, {item.state} · {item.proposalCount} de {item.maximumProposals}</Text></View>)}</Screen>; }
const styles = StyleSheet.create({ card: { padding: 17, gap: 8, borderRadius: 19, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, title: { color: colors.text, fontSize: 18, fontWeight: '800' }, description: { color: colors.textMuted }, meta: { color: colors.primary, fontSize: 12, fontWeight: '700' } });
