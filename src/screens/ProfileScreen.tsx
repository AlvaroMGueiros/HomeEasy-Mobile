import { StyleSheet, Text, View } from 'react-native'; import { NavigationProp, useNavigation } from '@react-navigation/native';
import { useAuth } from '../auth/AuthContext'; import { AppButton } from '../components/ui/AppButton'; import { Screen } from '../components/ui/Screen'; import { SectionHeader } from '../components/ui/SectionHeader'; import { RootStackParamList } from '../navigation/types'; import { colors } from '../theme/colors';
export function ProfileScreen() { const { user, logout } = useAuth(); const navigation = useNavigation<NavigationProp<RootStackParamList>>(); return <Screen><SectionHeader eyebrow="Minha conta" title={user?.name || 'Perfil'} description={user?.email} /><View style={styles.card}><View style={styles.avatar}><Text style={styles.avatarText}>{user?.name.charAt(0)}</Text></View><Text style={styles.role}>Conta Home Easy</Text></View><AppButton label="Ver oportunidades profissionais" variant="secondary" onPress={() => navigation.navigate('Opportunities')} /><AppButton label="Sair da conta" onPress={logout} /></Screen>; }
const styles = StyleSheet.create({ card: { alignItems: 'center', gap: 12, padding: 25, borderRadius: 22, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, avatar: { width: 86, height: 86, borderRadius: 43, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center' }, avatarText: { color: colors.white, fontSize: 32, fontWeight: '900' }, role: { color: colors.textMuted } });
